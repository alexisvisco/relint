package fmtfix

import "fmt"

type (
	// Bar is a type.
	Bar struct{} // want `FMTFIX: apply format fixes \(merge declaration blocks, reorder declarations\)`

	// Baz is a type.
	Baz struct{}
)

const (
	Alpha = "a"

	Beta = "b"
)

var (
	One = 1

	Two = 2
)

// Foo does something.
func Foo() { fmt.Println() }
